#include "binary.h"
#include <math.h>

namespace binary {

	long convert(std::string binary_number) {
		long position = 0;
		long dec_number = 0;
		
		while (position < binary_number.length()) {
			if (binary_number % 10 == 1) {

			}
		}

		return dec_number;

	}

}